# 2026-05-25 Codex 的交互模式

## 1. 文档信息

- 学习主题：Codex CLI 的 interactive mode，以及如何读懂终端交互工作流的英文说明
- 原文链接：https://developers.openai.com/codex/cli/features
- HTML 学习页：[[2026-05-25 Codex 的交互模式.html]]
- 原文词数：91（基于官方页面信息改写的英文精读材料；官方短摘录 17 词）
- 学习目标：读懂 interactive mode、terminal UI、repository、composer、in real time、approve / reject 等技术文档高频表达
- 难度评级：★★☆☆☆

> 说明：本文基于 OpenAI 官方 Codex CLI Features 页面生成。为避免大段复制公共网页原文，官方原文只保留一个短摘录；主要精读材料是根据官方页面信息改写的英文学习段落，不是官方逐字原文。

## 2. 本次精读原文

**官方短摘录：**

> Codex launches into a full-screen terminal UI that can read your repository, make edits, and run commands.

**本次英文精读材料（基于官方页面信息改写）：**

Codex interactive mode starts when you run the codex command in your terminal. It opens a full-screen terminal UI, where Codex can read your repository, make edits, and run commands while you work with it. You can also start a session with an initial prompt, such as asking Codex to explain a codebase. During the session, you send prompts, code snippets, or screenshots into the composer. Before changing files, Codex can explain its plan, and you can review its actions in real time, then approve or reject steps inline.

## 3. 自然中文翻译

当你在终端里运行 `codex` 命令时，Codex 的交互模式就会启动。它会打开一个全屏终端界面，在这个界面里，Codex 可以读取你的代码仓库、修改文件、运行命令，并且和你一起推进任务。你也可以在命令行里带上一个初始提示词来开始会话，比如让 Codex 解释一个代码库。在会话过程中，你可以把提示词、代码片段或截图发送到输入框里。在修改文件之前，Codex 可以先解释它的计划；你可以实时审查它的操作，然后在界面内批准或拒绝这些步骤。

## 4. 逐句精读

### 句子 1

**英文原句：**

<span class="proper-noun">Codex</span> <span class="tech-term">interactive mode</span> <span class="action-verb">starts</span> when you <span class="key-phrase">run the codex command</span> in your <span class="tech-term">terminal</span>.

**自然翻译：**

当你在终端里运行 `codex` 命令时，Codex 的交互模式就会启动。

**句子主干：**

Codex interactive mode starts.

**结构拆解：**

`when you run...` 是时间条件，说明“什么时候启动”。主句先说模式启动，后面再说明触发动作：你在终端里运行 `codex` 命令。

**技术关键词：**

- interactive mode：交互模式，强调你和工具来回对话、共同推进任务。
- run the codex command：执行 `codex` 这个命令。
- terminal：终端，输入命令和查看输出的地方。

**大白话解释：**

这句讲入口：不是点网页按钮，而是在终端里运行 `codex`，进入可以持续对话的模式。

### 句子 2

**英文原句：**

It <span class="action-verb">opens</span> a <span class="key-phrase">full-screen terminal UI</span>, <span class="sentence-pattern">where</span> <span class="proper-noun">Codex</span> can <span class="action-verb">read</span> your <span class="tech-term">repository</span>, <span class="key-phrase">make edits</span>, and <span class="key-phrase">run commands</span> while you work with it.

**自然翻译：**

它会打开一个全屏终端界面，在这个界面里，Codex 可以读取你的代码仓库、修改文件、运行命令，并且和你一起推进任务。

**句子主干：**

It opens a UI.

**结构拆解：**

逗号后面的 `where Codex can...` 用来补充说明这个 UI 里能发生什么。后面三个动词并列：`read`、`make`、`run`，分别对应读仓库、改文件、跑命令。

**技术关键词：**

- full-screen terminal UI：全屏终端界面，不是普通网页。
- repository：代码仓库，通常就是一个项目的代码集合。
- make edits：做修改，常指修改文件内容。
- run commands：执行命令。

**大白话解释：**

这句讲交互模式的工作台：Codex 在终端界面里接近你的代码，不只是聊天，还能实际操作。

### 句子 3

**英文原句：**

You can also <span class="key-phrase">start a session with</span> an <span class="tech-term">initial prompt</span>, such as asking <span class="proper-noun">Codex</span> to <span class="action-verb">explain</span> a <span class="tech-term">codebase</span>.

**自然翻译：**

你也可以带着一个初始提示词来开始会话，比如让 Codex 解释一个代码库。

**句子主干：**

You can start a session.

**结构拆解：**

`with an initial prompt` 说明开始会话时附带什么；`such as...` 给例子，说明这个 prompt 可以长什么样。

**技术关键词：**

- session：一次会话，从打开交互模式到结束这一段。
- initial prompt：初始提示词，一开始就交给工具的任务说明。
- codebase：代码库，一个项目或产品的整体代码。

**大白话解释：**

这句讲启动方式：你可以先进 Codex 再提问，也可以启动时就把第一条任务一起带进去。

### 句子 4

**英文原句：**

During the <span class="tech-term">session</span>, you <span class="action-verb">send</span> <span class="tech-term">prompts</span>, <span class="tech-term">code snippets</span>, or <span class="tech-term">screenshots</span> into the <span class="tech-term">composer</span>.

**自然翻译：**

在会话过程中，你可以把提示词、代码片段或截图发送到输入框里。

**句子主干：**

You send prompts into the composer.

**结构拆解：**

`During the session` 是时间背景。句子中间列了三个可以发送的东西：`prompts`、`code snippets`、`screenshots`。`into the composer` 说明发送到哪里。

**技术关键词：**

- prompt：提示词，给 AI 的任务说明。
- code snippet：代码片段，不一定是完整文件。
- screenshot：截图，可以作为视觉输入。
- composer：输入框 / 编辑区，用来组织你要发送的内容。

**大白话解释：**

这句讲你能给 Codex 什么输入：不只是文字问题，也可以给代码片段和截图。

### 句子 5

**英文原句：**

<span class="sentence-pattern">Before changing files</span>, <span class="proper-noun">Codex</span> can <span class="action-verb">explain</span> its <span class="tech-term">plan</span>, and you can <span class="key-phrase">review its actions</span> <span class="key-phrase">in real time</span>, then <span class="action-verb">approve</span> or <span class="action-verb">reject</span> steps <span class="confusing-word">inline</span>.

**自然翻译：**

在修改文件之前，Codex 可以先解释它的计划；你可以实时审查它的操作，然后在界面内批准或拒绝这些步骤。

**句子主干：**

Codex can explain its plan, and you can review its actions.

**结构拆解：**

`Before changing files` 放在句首，强调修改前先做什么。`and` 连接两件事：Codex 解释计划；你审查操作。最后的 `then approve or reject...` 表示你接着做决定。

**技术关键词：**

- plan：计划，工具准备怎样完成任务。
- review its actions：审查它的操作。
- in real time：实时，事情发生时就能看见和判断。
- approve / reject：批准 / 拒绝。
- inline：在当前界面或当前流程里，不是跳到别处。

**大白话解释：**

这句讲安全感来源：Codex 不是悄悄改文件，你可以看到它打算怎么做，并且实时决定放行还是拦下。

## 5. 技术词汇表

| 英文 | 本文意思 | 普通意思 | 技术文档常见意思 | 原文例句 | 记忆提示 |
|---|---|---|---|---|---|
| interactive mode | 交互模式 | interactive = 互动的 | 用户和工具来回输入、反馈、确认的运行模式 | Codex interactive mode starts... | interactive 重点是“来回互动” |
| terminal UI | 终端界面 | UI = 用户界面 | 在终端里显示的操作界面 | full-screen terminal UI | UI 不一定是网页 |
| repository | 代码仓库 | 仓库、储藏处 | 存放项目代码和历史记录的地方 | read your repository | repo = 项目代码集合 |
| make edits | 修改文件 | edit = 编辑 | 对代码或文档做具体改动 | make edits | make edits = 动手改 |
| run commands | 执行命令 | command = 命令 | 在终端里运行某条命令 | run commands | run = 执行，不是跑步 |
| session | 会话 | 一段时间 / 会议 | 一次持续的交互过程 | During the session | 打开到关闭算一次 |
| initial prompt | 初始提示词 | initial = 最初的 | 启动任务时给模型的第一条指令 | an initial prompt | initial = 一开始就带上 |
| codebase | 代码库 | 无常见口语义 | 一个项目的整体代码 | explain a codebase | base = 基础集合 |
| code snippet | 代码片段 | snippet = 小片段 | 一小段示例或相关代码 | code snippets | snippet 不等于完整文件 |
| screenshot | 截图 | 屏幕截图 | 作为视觉输入的图片 | screenshots | screen + shot |
| composer | 输入框 / 编辑区 | 作曲家 | 组织并发送消息的输入区域 | into the composer | compose = 组织内容 |
| in real time | 实时 | 真实时间 | 事情发生时马上看到或处理 | review its actions in real time | real time = 不等事后 |
| approve | 批准 | 同意、认可 | 允许某个步骤继续执行 | approve steps | approve = 放行 |
| reject | 拒绝 | 拒绝 | 阻止某个步骤继续执行 | reject steps | reject = 拦下 |
| inline | 在当前流程内 | 排成一行 | 不跳出当前界面或上下文 | steps inline | in-line = 在这一行/流程里 |

## 6. 重点短语

| 短语 | 中文意思 | 为什么重要 | 自己造句 | 造句翻译 |
|---|---|---|---|---|
| run the codex command | 运行 codex 命令 | 说明 CLI 工具的启动方式 | Run the codex command in your project folder. | 在你的项目文件夹里运行 codex 命令。 |
| full-screen terminal UI | 全屏终端界面 | 说明交互模式不是普通命令输出 | The tool opens a full-screen terminal UI. | 这个工具会打开一个全屏终端界面。 |
| read your repository | 读取你的代码仓库 | 说明 Codex 能理解项目上下文 | Codex can read your repository before making edits. | Codex 可以先读取你的代码仓库，再做修改。 |
| make edits | 做修改 | AI 编码工具最常见动作之一 | The agent can make edits to several files. | 这个代理可以修改多个文件。 |
| run commands | 执行命令 | CLI 场景里的核心能力 | You can let Codex run commands after review. | 审查之后，你可以让 Codex 执行命令。 |
| start a session with | 用某个内容开始会话 | 读懂启动参数和初始 prompt 很关键 | Start a session with a clear task. | 用一个清楚的任务开始会话。 |
| initial prompt | 初始提示词 | AI 工具文档高频表达 | An initial prompt helps Codex understand your goal. | 初始提示词能帮助 Codex 理解你的目标。 |
| review actions in real time | 实时审查操作 | 体现交互模式的控制感 | Developers review actions in real time. | 开发者会实时审查操作。 |

## 7. 高频句式

### 句式 1：X starts when you...

意思：当你做某个动作时，X 就开始。

例句：

- Codex interactive mode starts when you run the codex command.
  → 当你运行 `codex` 命令时，Codex 交互模式就会启动。
- The setup starts when you install the package.
  → 当你安装这个包时，设置流程就会开始。
- The session starts when you send the first prompt.
  → 当你发送第一条提示词时，会话就开始了。

还能这样用：

技术文档经常用这个句式解释“触发条件”。读的时候先抓 `X starts`，再看 `when you...` 后面的动作。

### 句式 2：It opens a Y, where X can...

意思：它打开一个 Y，在那里 X 可以做某些事。

例句：

- It opens a terminal UI, where Codex can read your repository.
  → 它打开一个终端界面，在那里 Codex 可以读取你的代码仓库。
- It opens a dashboard, where users can review logs.
  → 它打开一个仪表盘，用户可以在那里查看日志。
- It opens an editor, where you can change the file.
  → 它打开一个编辑器，你可以在那里修改文件。

还能这样用：

`where` 不一定只表示地点，它也常把“界面 / 环境”和“在里面能做什么”连接起来。

### 句式 3：You can also start X with Y

意思：你也可以用 Y 来开始 X。

例句：

- You can also start a session with an initial prompt.
  → 你也可以用一个初始提示词来开始会话。
- You can also start the app with a config file.
  → 你也可以用配置文件来启动应用。
- You can also start the command with extra flags.
  → 你也可以带上额外参数来运行这个命令。

还能这样用：

看到 `also`，通常说明文档在补充另一种用法；`with` 后面常常是工具、参数或附带内容。

### 句式 4：Before doing X, Y can...

意思：在做 X 之前，Y 可以先做某件事。

例句：

- Before changing files, Codex can explain its plan.
  → 在修改文件之前，Codex 可以解释它的计划。
- Before running commands, the tool can ask for approval.
  → 在执行命令之前，这个工具可以请求批准。
- Before deploying the app, you can review the changes.
  → 在部署应用之前，你可以审查这些修改。

还能这样用：

这个句式经常出现在安全、审批、工作流说明里。`Before...` 帮你判断步骤顺序。

## 8. 本文真正要掌握的内容

- 你要掌握的词：interactive mode、terminal UI、repository、session、initial prompt、codebase、composer、approve、reject、inline
- 你要掌握的短语：run the codex command、full-screen terminal UI、read your repository、make edits、run commands、review actions in real time
- 你要掌握的句式：`X starts when you...`、`It opens a Y, where X can...`、`You can also start X with Y`、`Before doing X, Y can...`
- 你要能用中文说清楚的问题：Codex 的 interactive mode 是什么？为什么它叫交互模式？你能给它哪些输入？它改文件前你能做什么审查？

## 9. 一句话总结

Codex 的交互模式就是在终端里打开一个全屏工作界面，让你和 Codex 边对话边看计划、审查操作、批准或拒绝修改。
